#include <stdio.h>

/**
 * main - entry point
 * @argc: argument count
 * @argv: argment character array of a string
 * Return: 0 success
 */
int main(int argc, __attribute__((unused))char *argv[])
{
	printf("%i\n", argc - 1);

	return (0);
}
